<div id="url-bar" class="input-group">
    <span class="input-group-btn">
        <button class="btn btn-default" type="button" disabled={{backDisabled}} {{action 'back'}}>
            <span class="fa fa-arrow-left"></span>
        </button>
        <button class="btn btn-default" type="button" disabled={{forwardDisabled}} {{action 'forward'}}>
            <span class="fa fa-arrow-right"></span>
        </button>
    </span>
    {{input class="form-control" placeholder="Enter a website address" value=(mut url) focus-in=(action 'restoreInputUrl') focus-out=(action 'revertInputUrl') disabled=disabled}}
    <span class="input-group-btn">
        <button class="btn btn-default" type="submit" disabled={{disabled}}>
            <span class="fa {{if loading 'fa-spin fa-refresh' (if urlChanged 'fa-arrow-right' 'fa-repeat')}}"></span>
        </button>
    </span>
</div>
