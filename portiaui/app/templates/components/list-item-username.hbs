{{#list-item-selectable value=(mut username) valueAttribute='label' onChange=(action 'saveSpider') menuContainer="body" menuAlign="left" as |select|}}
    {{#select.header}}
        Select Username
    {{/select.header}}
    {{#each usernameOptions as |option|}}
        {{#select.item value=option action=(action select.setValueAndClose option)}}
            {{option.label}}
        {{/select.item}}
    {{/each}}
{{/list-item-selectable}}
