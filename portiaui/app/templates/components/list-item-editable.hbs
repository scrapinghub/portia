{{#if editing}}
    {{buffered-input class="input-list-item" value=(mut value) focused=editing change=change autoSelect=true spellcheck=spellcheck}}
{{else}}
    <span>{{value}}</span>
    {{icon-button icon='edit' action=(action 'startEditing') bubbles=false}}
{{/if}}
