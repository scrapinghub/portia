import ensurePromise from '../../../utils/ensure-promise';
import { module, test } from 'qunit';

module('Unit | Utility | ensure promise');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ensurePromise();
  assert.ok(result);
});
