import promises from '../../../utils/promises';
import { module, test } from 'qunit';

module('Unit | Utility | promises');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = promises();
  assert.ok(result);
});
