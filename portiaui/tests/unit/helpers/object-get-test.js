import { objectGet } from '../../../helpers/object-get';
import { module, test } from 'qunit';

module('Unit | Helper | object get');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = objectGet(42);
  assert.ok(result);
});
