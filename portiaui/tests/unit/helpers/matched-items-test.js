import { matchedItems } from '../../../helpers/matched-items';
import { module, test } from 'qunit';

module('Unit | Helper | matched items');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = matchedItems(42);
  assert.ok(result);
});
