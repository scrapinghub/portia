import { chainActions } from '../../../helpers/chain-actions';
import { module, test } from 'qunit';

module('Unit | Helper | chain actions');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = chainActions(42);
  assert.ok(result);
});
