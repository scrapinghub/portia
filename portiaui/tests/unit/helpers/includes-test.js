import { includes } from '../../../helpers/includes';
import { module, test } from 'qunit';

module('Unit | Helper | includes');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = includes(42);
  assert.ok(result);
});
