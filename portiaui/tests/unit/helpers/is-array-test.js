import { isArray } from '../../../helpers/is-array';
import { module, test } from 'qunit';

module('Unit | Helper | is array');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isArray(42);
  assert.ok(result);
});
