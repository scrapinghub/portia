import { arrayGet } from '../../../helpers/array-get';
import { module, test } from 'qunit';

module('Unit | Helper | array get');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = arrayGet(42);
  assert.ok(result);
});
