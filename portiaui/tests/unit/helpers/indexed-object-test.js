import { indexedObject } from '../../../helpers/indexed-object';
import { module, test } from 'qunit';

module('Unit | Helper | indexed object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = indexedObject(42);
  assert.ok(result);
});
