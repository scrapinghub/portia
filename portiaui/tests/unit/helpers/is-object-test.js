import { isObject } from '../../../helpers/is-object';
import { module, test } from 'qunit';

module('Unit | Helper | is object');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isObject(42);
  assert.ok(result);
});
