import { attributeAnnotation } from '../../../helpers/attribute-annotation';
import { module, test } from 'qunit';

module('Unit | Helper | attribute annotation');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = attributeAnnotation(42);
  assert.ok(result);
});
