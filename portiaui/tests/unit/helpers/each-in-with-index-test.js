import { eachInWithIndex } from '../../../helpers/each-in-with-index';
import { module, test } from 'qunit';

module('Unit | Helper | each in with index');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eachInWithIndex(42);
  assert.ok(result);
});
