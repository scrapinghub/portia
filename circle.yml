machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t scrapinghub/portia .

test:
  override:
    - docker run -d -p 9001:9001 scrapinghub/portia; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:9001
