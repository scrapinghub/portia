import Ember from 'ember';
import ToolboxStateMixinMixin from 'portia-web/mixins/toolbox-state-mixin';

module('ToolboxStateMixinMixin');

// Replace this with your real tests.
test('it works', function() {
  var ToolboxStateMixinObject = Ember.Object.extend(ToolboxStateMixinMixin);
  var subject = ToolboxStateMixinObject.create();
  ok(subject);
});
