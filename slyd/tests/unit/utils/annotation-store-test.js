import AnnotationStore from 'portia-web/utils/annotation-store';

module('annotation-store');

// Replace this with your real tests.
test('it works', function() {
  var result = new AnnotationStore();
  ok(result);
});
