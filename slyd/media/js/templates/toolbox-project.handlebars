<div style="margin:10px 0px 0px 10px">
	<div style="text-align:center;font-size:1.1em;margin-bottom:5px">
		{{#ifHasCapability 'rename_projects'}}
			{{#view ASTool.RenameTextField value=name}}
				<span class="editable-name">Project {{name}}</span>
			{{/view}}
		{{else}}
			<h4>Project {{name}}</h4>
		{{/ifHasCapability}}
	</div>

	<h4>Spiders</h4>
	<div>
		<div class="scrolling-container" style="max-height:600px">
			{{#each}}
				<div class="row">
					<div class="col-md-9 clickable-url">
						{{#bs-button clicked="editSpider" clickedParam=this type="light"}}
							{{this}}
						{{/bs-button}}
					</div>
					<div class="col-md-3">
						{{bs-button clicked="deleteSpider" clickedParam=this icon="fa fa-icon fa-trash" type="danger"}}
					</div>
				</div>
			{{else}}
				<h3>No spiders for this project.</h3>
			{{/each}}
		</div>
	</div>
	{{#ifHasCapability 'version_control'}}
		{{#if controller.hasChanges}}
			<div style="text-align:center;font-size:1.1em;margin-top:10px">
				{{#bs-button clicked="publishProject" icon="fa fa-icon fa-upload" type="primary"}}
					Publish changes
				{{/bs-button}}
				{{#bs-button clicked="discardChanges" icon="fa fa-icon fa-trash" type="danger"}}
					Discard Changes
				{{/bs-button}}
			</div>
		{{else}}
			{{#ifHasCapability 'deploy_projects'}}
				<div style="text-align:center;font-size:1.1em;margin-top:10px">
					{{#bs-button clicked="deployProject" icon="fa fa-icon fa-upload" type="primary" disabled=controller.isDeploying}}
						Deploy
					{{/bs-button}}
				</div>
			{{/ifHasCapability}}
		{{/if}}
	{{/ifHasCapability}}
</div>

